An interactive project of fraction calculator. The fraction class defines the format of a fraction and all operations with it, whereas the calculator class implements a robust interactive calculator.
